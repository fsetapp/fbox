import { assert } from "@esm-bundle/chai";
import { initStore, initFileStore } from "./test_helper.js"


describe("#removeFromDiff", () => {
  // removeFromDiff(currentStore, pulledDiff.removed)
})
describe("#insertFromDiff", () => {
  // insertFromDiff(currentStore, pulledDiff.added)
})
describe("#updateFromDiff", () => {
  // updateFromDiff(currentStore, pulledDiff.changed)
})
describe("#reorderFromDiff", () => {
  // reorderFromDiff(currentStore, pulledDiff.reorder)
})
describe("#mergeToBase", () => {
  // mergeToBase = (baseStore, pulledDiff)
})
